# Test only URLs

## this heading with https://example.com should work

# Test only inline code

## this heading with `code` should work

# Test mixed

## this heading has https://api.example.com and `npm install` elements